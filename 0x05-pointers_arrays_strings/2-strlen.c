#include <stdio.h>
#include <string.h>
/**
 * _strlen - function to check length of the string
 *
 * @s: parameter to be printed
 *
 * Return: Always void
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
